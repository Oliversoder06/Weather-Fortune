# ============================================================
# Weather Fortune - Example Environment File
# Copy this file to ".env" and fill in your own values
# ============================================================

# -------------------------------
# Web (Next.js frontend)
# -------------------------------
# Base URL of your API (FastAPI service)
NEXT_PUBLIC_API_URL=http://localhost:8000

# -------------------------------
# Database (PostgreSQL / Supabase)
# -------------------------------
# SQLAlchemy connection URL (async) for app runtime
DATABASE_URL=postgresql+asyncpg://postgres:<password>@<host>:5432/postgres

# Sync URL for Alembic migrations (psycopg2 driver)
SYNC_DATABASE_URL=postgresql://postgres:<password>@<host>:5432/postgres

# -------------------------------
# Cache (Redis)
# -------------------------------
# Redis connection string (used for caching, rate-limiting, etc.)
REDIS_URL=redis://redis:6379/0

# -------------------------------
# CORS / Security
# -------------------------------
# Allowed origins for cross-origin requests
CORS_ORIGINS=http://localhost:3000
